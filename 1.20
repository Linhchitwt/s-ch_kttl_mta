#include <iostream>
#include <math.h>
using namespace std;
int main (){
    int n,t=0,a,b;
    float c;
    cout << "nhap vao so n = ";
    cin >> n;
    cout << "cac bo so la : "<<endl;
    for (int a=0; a<= n; a++){
        for (b = a+1 ; b <= n; b++){
            c = pow((a*a + b*b),1/2);
             if (c == (int)c && b<=c ){
                 t++;
                 cout << a <<" "<<b <<" "<<c<<endl; 
             }
        }
    }
    cout <<"co tat ca "<<t<<" bo"<<endl;
}

